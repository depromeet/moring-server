name: Check test coverage and merge

on:
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "GitHub 에서 레포 받아오기"
        uses: actions/checkout@v3

      - name: "JDK17 준비하기"
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: "Gradle 준비하기"
        run: chmod +x gradlew

      - name: "Gradle 빌드"
        run: ./gradlew test jacocoTestReport -x test

      - name: "Codecov에 커버리지 업로드"
        uses: codecov/codecov-action@v4.0.1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ./build/reports/jacoco/test/jacocoTestReport.xml

#      - name: "테스트 커버리지 저장 xml 파일 확인"
#        run: |
#          if [ -f "${{ github.workspace }}/build/reports/jacoco/test/jacocoTestReport.xml" ]; then
#            echo "Jacoco report exists."
#          else
#            echo "Jacoco report does not exist."
#            exit 1
#          fi
#
#      - name: "테스트 커버리지를 PR에 코멘트로 등록"
#        id: jacoco
#        uses: madrapps/jacoco-report@v1.2
#        with:
#          title: 📝 테스트 커버리지 리포트입니다
#          paths: ${{ github.workspace }}/build/reports/jacoco/test/jacocoTestReport.xml
#          token: ${{ secrets.GITHUB_TOKEN }}
#          min-coverage-overall: 70
#          min-coverage-changed-files: 70